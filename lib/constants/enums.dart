enum Status {
  PENDING,
  ACCEPTED,
  IN_PROGRESS,
  DELIVERED,
  CONFIRMED,
  CANCELED,
  DECLINED
}

enum PackSize { Unknown, S, M, L, XL, XXL }

enum SortOption { Price, Distance, Date }

enum Lang { EN, RU }

enum ChatType { Deals, Users }

enum MenuItem { AllItems, SendNew, MyItems, Messages, Profile }

enum Country { RU, UA, BY, DE, CZ }
